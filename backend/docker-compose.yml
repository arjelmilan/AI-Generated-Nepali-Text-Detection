version: "3.8"
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: nepdetect
      POSTGRES_PASSWORD: nepdetect_password
      POSTGRES_DB: nepdetect_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
